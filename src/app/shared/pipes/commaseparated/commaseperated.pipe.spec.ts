import { CommaseperatedPipe } from './commaseperated.pipe';

describe('CommaseperatedPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaseperatedPipe();
    expect(pipe).toBeTruthy();
  });
});
